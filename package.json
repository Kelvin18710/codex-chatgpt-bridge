{
  "name": "codex-chatgpt-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local ChatGPT web consultation bridge and control panel for Codex",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "install:local": "node scripts/install.mjs",
    "uninstall:local": "node scripts/install.mjs --uninstall"
  }
}
