#!/usr/bin/env bash
set -eu
umask 077
bridge_state="$HOME/.local/state/codex-chatgpt-bridge"
mkdir -p "$bridge_state"
exec flock -n -E 75 "$bridge_state/consult.lock" node "$HOME/.local/share/codex-chatgpt-bridge/consult.mjs" "$@"
